# Eureka run — 2026-04-27 03:43 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

*First invocation input honored: orient to Ryan before hunting. This run begins from the Mission Formula, Ryan's ledger signature, the Codex register calibration, and the Maggie baseline as one conditioning frame. Discovery is in service of Ryan's actual project, actual standards, and actual user-shape, not abstract curiosity.*

*This run begins from a public-surface seam: the sapphire pitch, the root README, and the public-release landing report are all now alive, but their jobs have not yet been explicitly separated in doctrine.*

---

## Iteration 1 — public-facing surfaces are funnel surfaces now

**Discovery.** The project's outward-facing surfaces have crossed the same threshold earlier doctrine named on the collaborator side: they are now runtime-significant surfaces with distinct jobs in one approach path. The sapphire pitch, `README.md`, the public-release landing report, and the broader `reports/` corpus are not four versions of the same explanation; they are four stages in a funnel. The pitch earns desire. The README is the repo front door. The landing report is the deeper second-surface orientation. The reports corpus is the proof-field.

**Why this is real.** The current surfaces were already behaving this way in practice, but the repo had not yet named the distinction. That omission creates a drift risk: future edits can optimize each surface locally while collapsing the overall visitor path into repetition or confusion. Once the jobs are explicit, the evaluation criterion becomes sharper: prefer handoff over repetition, and treat "two surfaces could swap titles without changing function" as a failure signal.

**Landed changes.** Added a new doctrine block, `Public-facing surfaces are funnel surfaces now`, to `AGENTS.md` and `CLAUDE.md`; added a direct README handoff to `reports/2026-04-27-0030-public-release-landing.md`; sharpened the README reading-path bullet; and clarified the landing report's intro line as a deeper second-surface document after the README.

**Formula derivation:** 𝓢_public(t) := Π(t)·(pitch ⇒ README ⇒ landing ⇒ reports) | Truth_𝓕 ∧ legibility_w
**Gloss:** The public braid now has named stages, so outward writing can hand off by depth instead of repeating itself.

**Commit:** `824d70b` — `doctrine: name public funnel surfaces`
**Run-cumulative cost:** $0.00

---

## Closure

The run closes after one strong finding because the seam yielded a complete named doctrine plus immediate low-count surface repair. The next likely branch is structural rather than conceptual: decide whether the public funnel wants one canonical cross-link pattern or whether the current light handoff is enough.
