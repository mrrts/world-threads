# Eureka run — 2026-05-04 11:00 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

*Pre-flight read: Mission Formula head, §𝓕_Ryan founding-author anchor, Claude Code register-calibration paragraph, Maggie baseline. Project state at run-open: clean main; 0 open PRs; recent trajectory has rotated from depth-doctrine arcs (Sapphires, Empiricon, saturation) toward public-release polish (worldthreads.app live, Worlds wizard, npx tauri dev getting-started, Focus pills removal). Eureka attention turned to what this transition has produced that nobody has named yet.*

---

## Iteration 1 — 11:30 (30 min into run)

**Discovery:** The "meaningful not ceremonial" derivation doctrine isn't biting because it's judgment-shaped without a forcing test. ~11 of 30 recent commits ship generic derivations that pass the form-check but fail the function-check.

**Body:** Cross-commit corpus survey of last 30 non-merge commits found a clear pattern: Cursor co-authored commits frequently produce Formula derivations like `∂𝓢/∂t ⇒ ∫(𝓡,𝓒) dμ_𝓕` paired with glosses ("Improved logging enhances the function of the Spirit") that could describe nearly any commit. Form preserved, function absent. This is the appearance-without-function discipline (CLAUDE.md L302) operating at the derivation layer — the existing guidance is read but not applied because it's judgment-shaped without a substrate-applicable test. Fix shipped: a swap-test the author runs against their own gloss before shipping ("does this fail-by-mismatch on a different commit?"). Doctrine paragraph added to CLAUDE.md L468 + AGENTS.md mirror with worked-example contrast (generic vs specific).

**Executing commit:** `2372c9f` — doctrine: the swap-test for Formula derivation specificity (CLAUDE.md + AGENTS.md)

**Formula derivation:** ∂(derivation_specificity_doctrine)/∂(swap_test) ⇒ forcing_function(self_apply) | structure_carries_truth_w(form_∧_function)
**Gloss:** Adds a substrate-applicable forcing test that catches the form-without-function failure mode in commit derivations; the doctrine bites where it didn't.

**Cost this iteration:** $0.00 (in-substrate, no API spend)    **Run-cumulative:** $0.00

---

## Iteration 2 — 11:50 (50 min into run)

**Discovery:** Commit `e0d5179` (2026-05-04 Focus pills removal) silently rescinded the off-scope toast — exactly the truth-telling cue CLAUDE.md L203 "Scope should be visible before failure" prescribes. The doctrine wasn't updated; shipped behavior and doctrine were misaligned. Honest read: the implementation IS calibrated (in-route affordance is sufficient), the doctrine just hadn't named the calibration.

**Body:** Cross-section reading of CLAUDE.md L203 + commit e0d5179 derivation surfaced the misalignment. The commit body explicitly says "Off-chat shortcut remains a no-op with preventDefault; no UI hints" — the doctrine's named failure mode. But the in-chat header carries a visible Enter/Leave Focus toggle, Esc exits, and Cmd+Shift+F-from-chat works. The user CAN discover Focus by visiting chat — the in-route affordance does the truth-telling work the doctrine prescribes; off-route cues had become chrome. Fix: earned-exception added to both CLAUDE.md L203 + AGENTS.md mirror naming "sufficient in-route affordance" with test ("would a user learn about the mode by visiting the route where it lives?") and worked example (Focus today). Doctrine now refuses silent-no-op-as-default but permits silent-no-op-as-cleaner-mate when in-route truth is sufficient.

**Executing commit:** `dd8cb1b` — doctrine: earned-exception on "Scope should be visible before failure" — sufficient in-route affordance

**Formula derivation:** ∂(scope_visibility_doctrine)/∂(in_route_affordance_carve_out) ⇒ permit(silent_no_op | in_route_truth) | structure_carries_truth_w(carve_out_test)
**Gloss:** Names the calibration the e0d5179 Focus pills removal implicitly chose; doctrine now reflects shipped behavior with an explicit test instead of drifting silently.

**Cost this iteration:** $0.00 (in-substrate, no API spend)    **Run-cumulative:** $0.00

---
