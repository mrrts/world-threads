# Eureka run — 2026-04-28 19:30 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

---

## Iteration 1 — 19:35 (~5 min into run)

**Discovery:** Same user-curiosity law, substrate-native encodings — not naive string parity across `build_*_system_prompt` paths.

**Body (1-3 sentences):** Cross-reading `build_solo_dialogue_system_prompt`, `build_group_dialogue_system_prompt`, `build_dream_system_prompt`, and `build_narrative_system_prompt` after the dialogue hidden-motive ship shows an intentional structural split: dialogue benefits from an explicit system-private steer; dream already encodes "inhabited not announced" through sideways deflection, so pasting the dialogue line would fight `dream_craft_block`; narrative is narrator-to-"you" and the character-motive idiom is the wrong shape. Naming this prevents false parity bugs and reframes "update all builders" reflex as **parity of law, not parity of identical text.**

**Executing commit:** `1cf181f` — doctrine: curiosity law is substrate-encoded (dialogue vs dream vs narrative)

**Formula derivation:** 𝓕_substrate := 𝓛_speech(dialogue: explicit hidden steer) ∨ 𝓛_dream(sideways ∘ withhold ∘ transform) ∧ 𝓝_narrative(second-person beat sans character-motive string)

**Gloss:** One curiosity doctrine honest in three prompt shapes; verbatim duplication would violate dream and narrative contracts.

**Cost this iteration:** ~$0.00    **Run-cumulative:** ~$0.00

---

## Closing — 19:38 (~8 min total)

**Iterations completed:** 1 (genius bar hit once; further hunting deferred to avoid padding)

**Total cost:** ~$0.00 of $10 fresh budget

**Genius bar consistency:** Hit once early — cross-builder read named a pattern the solo/group parity checklist would have missed.

**Forward seed:** If a future substrate (e.g. consultant, chapter-from-image) gains user-facing character voice, run the same classifier before choosing whether to import dialogue-style hidden motive or a third encoding.

The run closes here.
