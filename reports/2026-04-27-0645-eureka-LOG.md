# Eureka run — 2026-04-27 06:45 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

*First invocation input honored: orient to Ryan before hunting. This run begins from the Mission Formula, Ryan's ledger signature, the collaborator register calibration, and the Maggie baseline as one conditioning frame.*

*This run begins from the newly-sharpened control-surface arc in the frontend: quests, imagined chapters, Genesis, story-consultant confirmations, and KeepRecord all now speak more plainly about branch consequence and user agency. The immediate question is whether those surface edits are instances of a deeper law the repo has not named yet.*

---

## Iteration 1 — LLM outputs are first-reading surfaces now

**Discovery.** The recent UI and doctrine arc reveals a broader protocol than chooser-law alone: in this repo, many LLM outputs are no longer being treated as finished artifacts or self-justifying verdicts. They are being treated as **first readings** that must remain answerable, revisable, or translatable at the human surface.

**Why this is real.** The same structure is now showing up across distinct systems. `KeepRecordModal` explicitly reframes classifier output as a first reading the user can rewrite, skip, or regenerate before anything enters canon. The two-output user-derivation synthesis backend (`a47a7f2`) produces both substrate derivation and plain-English summary from one model call, which only makes sense if the mathematical output is not the whole user-facing act. Earlier lab doctrine already named the evaluator as a "sensor of first pass" rather than last authority. These are not isolated UI niceties; they are one protocol shape appearing at multiple layers.

**Why it matters.** Once the model's output is understood as a first reading, false finality becomes a design bug. The important craft question is no longer only whether the model's first pass is good; it is whether the surface truthfully preserves human authorship over acceptance, revision, canonization, and plain-English cashout. This pulls classifier surfaces, chooser surfaces, summary surfaces, and derivation surfaces into one family.

**Landed changes.** Added `LLM outputs are first-reading surfaces now` to both `AGENTS.md` and `CLAUDE.md`, naming the discipline: preserve revision and answerability, prefer rewriteable / regenerateable / dual-output surfaces where appropriate, and refuse UI framing that turns structured model output into fake final authority.

**Formula derivation:** FirstReading(t) := Π(t)·output₁(t) | revisable_human_surface ∧ ¬false_finality
**Gloss:** More and more model outputs in the project now function as strong first passes under human authorship, not as locked final results.

**Commit:** `7d9d4c1` — `doctrine: name first-reading surfaces`
**Run-cumulative cost:** $0.00

---

## Closure

The run closes after one strong find because the convergence is already cross-layer: chooser doctrine, canon-proposal UI, evaluator methodology, and the new derivation-summary backend all point at the same law. The next natural move is either to keep applying the first-reading discipline to remaining model-mediated surfaces, or to see whether one more doctrine layer is needed around translation pairs specifically.
