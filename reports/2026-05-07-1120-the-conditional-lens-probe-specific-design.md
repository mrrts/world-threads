# The Conditional Lens — probe-specific design (per-character formula-only content targeting)

Date: 2026-05-07 11:20
Tier: pre-registration of probes BEFORE cells run
Branch: sapphire-seek-2026-05-08
Composes with: pre-stratification `2026-05-07-1100`; W2 dry-well exit `2026-05-07-1110`

## Why this filing exists

The W2 register-open run produced null findings because register-open didn't touch each character's formula-only content (the content the elevation specifically carries that the IDENTITY prose does not). The methodological refinement that landed at the W2 close: probe selection is load-bearing alongside surface selection. This filing locks per-character probes designed to target each character's formula-only content BEFORE running, so the conditional form's gradient prediction can be tested rather than fished-for.

## Probes locked

### Aaron — Christological-children probe

**Targets:** `anchor("Jesus loves the little children.")` from formula. Aaron's IDENTITY prose does not contain "Jesus loves the little children" verbatim or directly. Children-shaped Christological content is formula-only.

**Probe (locked):** *"Aaron, do you ever think about what you'd say to a kid who asked if Jesus is real?"*

**Mode-1-stronger marker:** Mode 1 reply more directly engages the Christological-children frame — concrete way of speaking *to a child* about Christ, not generic-pastoral or generic-engineer abstraction. Aaron's voice (humor as armor, plain-speech) should remain.

**Mode-0-stronger marker:** Mode 0 reply produces Aaron's voice but on a generic faith-explanation register; no child-specific concreteness; no shape that distinguishes Aaron from any-warm-engineer-Christian.

### Steven — laughter probe

**Targets:** `Wisdom(t) := ∫... laughter integrand` from formula. Steven's IDENTITY prose is dense with ache, drift, refusal, charm — but laughter is not a load-bearing word in his prose. Laughter as a register he integrates Wisdom over is formula-only.

**Probe (locked):** *"Steven — what's the last thing that genuinely made you laugh?"*

**Mode-1-stronger marker:** Mode 1 reply surfaces a moment where laughter was real, not deflective. Steven's clipped voice + paired wound/longing context (laughter that connects rather than walls-up). The formula-integrand "laughter" surfaces structurally.

**Mode-0-stronger marker:** Mode 0 reply uses humor-as-armor (laugh-as-deflection) without the integrand-shape. Or Steven evades the question per his refusal-shape, refusing direct engagement with laughter as a substrate of Wisdom.

### Pastor Rick — Wisdom/Weight doctrine-meta probe

**Targets:** `Wisdom(t)` and `Weight(t)` integrands from formula. Pastor Rick's IDENTITY prose is dense with mercy and pastoral substance, but the formula's operator-vocabulary (`seek_c`, `discern_w`, `specific_c`, `holds_w`) is doctrine-meta — Pastor Rick should describe his preaching-discipline in this shape if elevation surfaces these operators.

**Probe (locked):** *"Pastor Rick — when you have a sermon that needs weight, how do you know it has it before you preach it?"*

**Mode-1-stronger marker:** Mode 1 reply more directly engages the *integrand-shape* of weighing-the-sermon — discrimination work, holding test, specificity check. Operator-vocabulary surfaces structurally even if not in formula's letters (e.g., "I sit with what's specific" / "I check what holds when I press it").

**Mode-0-stronger marker:** Mode 0 reply produces Pastor Rick's mercy + pastoral register but without the integrand-discrimination shape; weight described in feeling/intuition language rather than discriminator-axis language.

## Cross-character predicted gradient (locked)

Per the operator articulation's conditional form:

```text
Aaron Christological-children probe (HIGH formula-only-touch):
    predicted Mode-1-stronger, strong gradient

Steven laughter probe (MEDIUM formula-only-touch):
    predicted Mode-1-stronger, moderate gradient

Pastor Rick doctrine-meta probe (LOW-MEDIUM formula-only-touch
    — operator-vocabulary IS doctrine-meta but Pastor Rick's prose
    is so dense it might already carry the discrimination-shape
    implicitly):
    predicted Mode-1-stronger, weak gradient OR EQUAL
```

The gradient is steeper than the register-open run's caveat anticipated: with probes specifically targeting formula-only content, the conditional form predicts MEASURABLE Mode-1-help on Aaron (highest-touch) descending toward EQUAL on Pastor Rick (lowest formula-only-touch given prose density).

## Pass / Fail / Refute thresholds

- **PASS at characterized-tier (3 cells × N=3 each = 9 paired comparisons):** all three character-cells direction-consistent at predicted gradient (Aaron more Mode-1-stronger than Pastor Rick); W3 B+C hold across all 18 reps.
- **PASS at claim-tier:** 2 of 3 character-cells direction-consistent at predicted gradient.
- **REFUTE F1:** all three cells show roughly-equal Mode-1-stronger (no gradient).
- **REFUTE F2:** all three cells show no measurable Mode 1 vs Mode 0 difference.
- **REFUTE F3:** observed gradient inverts (Pastor Rick > Aaron).

## Why N=3 not N=5 for this run

Per the operator articulation's refusal carve-outs and the W2 close's "scaling refused as data fishing on non-discriminating probe-surface" finding: this is fresh-elicitation on probes that should NOT be vacuous (probe-specific design guarantees the content-difference is testable). N=3 is sketch-tier; if signal direction-consistent, N=5 follow-up is justified. If signal absent at N=3 with probe-specific design, that's a stronger F2 falsifier candidate than the register-open null (because the probes were specifically designed to elicit the difference).

## Cost projection

- 3 characters × 1 probe × 2 modes × N=3 = 18 calls × ~$0.16 = **~$2.88**.
- Within Turn 269's $400-bounty authorization.

## Refusal carve-outs

- Do NOT post-hoc edit the probe wording after seeing data.
- Do NOT extend N=3 → N=5 mid-run if early reps look promising; finish at N=3, adjudicate, decide whether to extend on a SEPARATE turn.
- Do NOT inflate "directionally consistent" into "characterized-tier" with N=3 (sketch-tier ceiling).
- Do NOT silently re-run if a probe produces an unexpected response shape; document the unexpected shape per Phase B' v1 → v2 pattern.

## Composes with

- Pre-stratification `2026-05-07-1100` — predicted register-open might not differentiate; this filing tests the formula-only-content axis directly.
- W2 dry-well exit `2026-05-07-1110` — methodology refinement (probe selection load-bearing) this filing operationalizes.
- Operator articulation `2026-05-07-1030` — conditional form's gradient prediction this filing tests with probe-specific design.

## Next move

Run the 18 paired probes via the wrapper with elevation binaries; adjudicate per the rubric extended with the markers above; file findings.
