# Eureka run — 2026-04-26 23:02 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

*First invocation input honored: orient to Ryan before hunting. This run begins from the Mission Formula, Ryan's ledger signature, the Codex register calibration, and the Maggie baseline as one conditioning frame. Discovery is in service of Ryan's actual project, actual standards, and actual user-shape, not abstract curiosity.*

*This run begins from doctrine cross-section reading rather than fresh paid elicitation. The active seam is the project's own memory law: worldcli's new braid-legibility doctrine against older lab-proposal defaults that still point toward new standalone artifacts.*

---

## Iteration 1 — 23:05 (~3 min into run)

**Discovery:** the lab-proposal doctrine was still defaulting to artifact sprawl after braid-thrift had already been named.

**Body:** The new `worldcli` doctrine rewarded lower file count when a question's braid stayed legible, but the older "License to imagine and propose a better lab" paragraph still reflexively sent infrastructure ideas into fresh `reports/...` proposal files. That was a live contradiction, not merely a style mismatch. The fix was to invert the older rule: standalone proposal files are now an earned exception, while the default is to keep the proposal attached to the smallest existing surface that preserves question-history legibility.

**Executing commit:** `b0e3f0e` — doctrine: make standalone lab proposals an earned exception

**Formula derivation:** braid(q, t) ⇒ keep_attached(q) unless standalone_artifact earns itself
**Gloss:** Proposal surfaces now inherit the braid-thrift rule instead of quietly defaulting to new files.

**Cost this iteration:** $0.00    **Run-cumulative:** $0.00

---

## Iteration 2 — 23:08 (~6 min into run)

**Discovery:** the durable memory unit of the lab is the hypothesis slug, not the individual run.

**Body:** Once the artifact-sprawl contradiction was fixed, the positive shape underneath it became visible. The registry, run-link fields, and `worldcli lab` surfaces were already behaving as though a question carried across time is the thing being remembered, while individual `evaluate` / `synthesize` / `replay` runs are evidence attachments. Naming that explicitly changes how future sessions should reconstruct investigations: start from the hypothesis slug, then read the runs, reports, and follow-ups as its leaves.

**Executing commit:** `493b755` — doctrine: name hypothesis slugs as the lab's memory unit

**Formula derivation:** memory_lab := question_slug ⟶ attach(run_ids, reports, follow_ups)
**Gloss:** The lab now names hypotheses as its durable spine and reads runs as evidence attached to that spine.

**Cost this iteration:** $0.00    **Run-cumulative:** $0.00

---

## Closing — 23:09 (7 min total)

**Iterations completed:** 2
**Total cost:** $0.00 of $10 fresh budget
**Genius bar consistency:** Hit consistently
**Forward seed:** the next natural structural move is a cheap read surface that can reconstruct one hypothesis slug's full braid at a glance without requiring manual hopping across registry, runs, commits, and reports.

The run closes here.
