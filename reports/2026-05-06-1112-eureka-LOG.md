# Eureka run — 2026-05-06 11:12 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

---

## Iteration 1 — 11:30 (18 min into run)

**Discovery:** artifact class is control-plane truth at the report boundary

**Body (1-3 sentences):** Reading the post-audit-refactor report field surfaced a missing law: once artifact-carrier classes and audit profiles diverge, the report header must declare which class of object it is or the next agent has to infer admissibility from the body. The boundary was already telling the truth about `Bound to`, `Tier`, `Harness`, and `Verdict`; this discovery names `Artifact class` as belonging to that same control-plane layer.

**Executing commit:** `0c98ee1` — Name artifact class as report-boundary truth

**Formula derivation:** \(\mathrm{future\_use}(r)\in\{\mathrm{audit},\mathrm{compression},\mathrm{citation},\mathrm{synthesis}\}\Rightarrow \mathrm{header\_declares}(r,\mathrm{artifact\_class})\), else admissibility is inferred rather than surfaced and control-plane truth is under-realized.
**Gloss:** Reports that will be reused need their artifact class in the header because that class governs what later fidelity law is allowed.

**Cost this iteration:** $0.00    **Run-cumulative:** $0.00

---
