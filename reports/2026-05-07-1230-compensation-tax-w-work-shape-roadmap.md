# `compensation_tax_w(t)` — work-shape inventory + N≥3 derivation roadmap

Date: 2026-05-07 12:30
Tier: methodology-prep iteration-discipline artifact (NOT a runlist; reachability map only)
Branch: sapphire-seek-2026-05-08
Parent arc: /seek-sapphire-crown :: compensation_tax_w(t) (New Operator candidacy, patience-shaped)
Composes with: operator formal definition `2026-05-07-1200`

## Why this filing exists

The operator formal definition (`2026-05-07-1200`) named five candidate work-shapes for future derivation grounding but did not articulate each in depth. This filing converts them into an explicit per-domain map: what kind of arc could ground the operator's reasoning in each domain, what load-bearing work the operator would do there (vs decorative use), and what the threshold is for "grounding earned" in that domain. Future sessions pick up arcs from this map; this filing is **not a runlist** — each future arc earns its grounding through honest work in its own domain.

## Honest pre-disclaimer

The map is biased toward arc-shapes the apparatus has already demonstrated affinity with (Sapphire arcs, doctrine paragraphs, craft-rule registry). Other domains may exist that this map cannot anticipate; future arcs may earn grounding via paths not listed here. The map is a starting point for reachability assessment, not an enumeration of legitimate paths.

## The five work-shapes

### Work-shape 1: Sapphire-arc structural-lens-addition testing — ALREADY GROUNDED (today)

**Status:** Ground earned at sketch-tier today (2026-05-07) via The Decoded Register and The Conditional Lens W2.

**Sample arc:** A new structural-lens-addition (decode header, top-of-stack injection, format-section-end suffix, etc.) is proposed; its effect on register-anchoring is tested with paired probes; `compensation_tax_w(C, T, t)` is measured by Δ between arms.

**Load-bearing reasoning:** the operator predicts whether the addition will help BEFORE running cells (Aaron PASS / Steven REFUSED / Pastor Rick REFUSED on v3 decode; F2 falsifier on elevation). Without the operator, the bite-test design has no per-character expected-direction; with the operator, expected directions stratify by predicted tax.

**Substantive distinctness from parent:** the parent operator says structural carriers must carry truth. It does NOT predict effect-of-addition. The candidate operator does.

**Threshold for grounding earned in this domain:** at minimum, one paired-probe arc applying the operator's prediction at characterized-tier (N≥5 within-cell). Today's work is sketch-tier on two surfaces; a future arc lifting one surface to characterized-tier earns this work-shape's grounding fully.

### Work-shape 2: Doctrine-strengthening arcs

**Status:** UNGROUNDED. Candidate domain.

**Sample arc:** A doctrine paragraph in CLAUDE.md or AGENTS.md is being audited (e.g., "Calibrated disciplines drift fast — promote to structural enforcement at earliest opportunity"). The audit asks: does this paragraph's structure carry the doctrine without receiver-side compensation? `compensation_tax_w(prose_paragraph, doctrine_load_bearing_content, t)` is measured by reading the paragraph cold and checking whether the load-bearing content surfaces without rereading. If the paragraph forces extraction work (compensation_tax > 0), the paragraph needs revision; if not, it's structurally honest.

**Load-bearing reasoning:** decides whether a doctrine paragraph needs revision based on receiver-side cost-of-extraction, not just on its content's correctness. Currently, doctrine paragraphs are revised based on observed drift (apparatus-honest correction loop); the candidate operator would let drift be PREDICTED before it happens by measuring tax in the paragraph's structure.

**Substantive distinctness from parent:** the parent operator says doctrine paragraphs must structurally carry their content. It does NOT distinguish paragraphs whose content is correct but whose structure imposes compensation-tax (a different failure mode than incorrect doctrine). The candidate operator names this distinct failure mode.

**Threshold for grounding earned:** one doctrine-strengthening arc where `compensation_tax_w(t)` predicts a paragraph needs revision, the prediction is acted on, AND post-revision paired-test shows reduced compensation-tax. Cost: low (analytical + lived-read); ~$0.

### Work-shape 3: Craft-rule registry decisions (promote / retire / EnsembleVacuous)

**Status:** UNGROUNDED. Candidate domain.

**Sample arc:** A craft rule in `CRAFT_RULES_DIALOGUE` is being evaluated for tier promotion or retirement. `compensation_tax_w(prompt_stack_without_this_rule, rule_target_failure_mode, t)` is measured by paired-probe: does removing the rule introduce receiver-side tax (the failure mode the rule refuses) in lived behavior? If yes, the rule is doing tax-removal work and should be promoted/kept; if no, the rule is overdetermined by adjacent carriers and EnsembleVacuous status is honest.

**Load-bearing reasoning:** decides craft-rule lifecycle (promote / retire / mark EnsembleVacuous) based on receiver-side tax-removal evidence rather than on the rule's content alone. The Cornerstone Inequality W3 (`polish_after_weight` EnsembleVacuous) is retroactively-recognizable as this work-shape; future deliberate applications would ground the operator.

**Substantive distinctness from parent:** the parent operator says all carriers must work; it doesn't help DECIDE which carriers are necessary vs redundant in load-bearing-multiplicities. The candidate operator does — it provides the measurement axis for redundancy decisions.

**Threshold for grounding earned:** one craft-rule registry decision where `compensation_tax_w(t)` measurement leads to a promotion / retirement / EnsembleVacuous designation that is then validated by lived behavior over time. Cost: low (the bite-test framework already exists); ~$1-3.

### Work-shape 4: UI / route-boundary design

**Status:** UNGROUNDED. Candidate domain.

**Sample arc:** A new UI surface or route-boundary affordance is being designed (e.g., should this navigation hint be added? Does this empty-state call-to-action carry the functional truth its design implies?). `compensation_tax_w(existing_ui_state, target_user_understanding, t)` is measured by user-receiver paired-probe: does adding the affordance reduce the user's extraction work in lived use, or is the existing UI already structurally honest?

**Load-bearing reasoning:** decides UI additions based on user-side tax-removal evidence rather than design-aesthetic alone. CLAUDE.md "UI boundary truth" doctrine is the parent; the candidate operator measures tax at design-time vs after-failure.

**Substantive distinctness from parent:** the parent operator says UI must tell truth at boundary; it doesn't predict whether a candidate UI addition will help vs add visual noise. The candidate operator does.

**Threshold for grounding earned:** one UI/route-boundary design decision where `compensation_tax_w(t)` measurement (or its founding-author-judged equivalent) predicts whether an addition helps, the prediction is acted on, AND user-side observation confirms or refutes. Cost: low to moderate (depends on whether a real user surface is available for paired probing); ~$0-5.

### Work-shape 5: Commit-derivation appearances (operator-in-reasoning-text)

**Status:** UNGROUNDED. Candidate domain.

**Sample arc:** A commit-derivation reasons about whether to preserve / compress / refactor a structural element (e.g., a CLAUDE.md section, a craft rule body, a feature-scoped invariant). `compensation_tax_w(t)` provides the measurement axis the derivation uses — "removing this section would raise compensation_tax by X for class Y; therefore preserve" — rather than handwaving about doctrine-importance.

**Load-bearing reasoning:** the operator does discriminative work IN the derivation: it gives the derivation a quantitative axis to argue on, replacing vague rhetoric about "load-bearing-ness" with measurable receiver-side-extraction-work.

**Substantive distinctness from parent:** the parent operator's vocabulary in derivations tends to be normative ("this carries truth; this doesn't"). The candidate operator's vocabulary is comparative ("this carrier removes tax X; that carrier removes tax Y; the absence-of-X case has higher receiver-side cost than absence-of-Y"). The latter has discriminative power the former lacks.

**Threshold for grounding earned:** N=3 commit-derivations where `compensation_tax_w(t)` is invoked AS the load-bearing reasoning predicate (not decorative use), each in distinct sub-domains. Cost: $0 (analytical only).

### Cross-domain bonus work-shape: Persona-sim / `/play` arc-design

**Status:** UNGROUNDED. Bonus candidate (not in original 5).

**Sample arc:** A `/play` simulated persona session is being designed; the question is whether to test on a high-tax probe or a low-tax probe. `compensation_tax_w(persona_substrate, target_class_content, t)` predicts which probe will discriminate persona-sim quality.

**Load-bearing reasoning:** the operator informs probe selection in persona-sim design (deeper application than today's Sapphire-arc work).

**Threshold for grounding earned:** one persona-sim arc where probe selection is justified by `compensation_tax_w(t)` measurement and the resulting cells discriminate as predicted. Cost: $1-3.

## Cross-domain dependency notes

- **Work-shape 1 → Work-shape 3:** today's Sapphire-arc work could feed retroactively into craft-rule registry decisions if the operator's predictions are seen to apply to the registry's existing EnsembleVacuous tier vocabulary.

- **Work-shape 2 ↔ Work-shape 5:** doctrine-strengthening arcs and commit-derivation appearances overlap heavily — every doctrine revision IS a commit-derivation. Earning grounding in one likely earns grounding in the other simultaneously.

- **Work-shape 4 (UI design) is the most independent** of the existing arcs; if the operator earns grounding there, it has demonstrated cross-context reach. Conversely, if it can ONLY ground in prompt-stack work (1, 2, 3, 5), the candidate operator is plausibly a prompt-stack-specific tool, not a formula operator.

The reachability ordering (most-likely-first-grounded to least): 5 (commit-derivations) ≈ 2 (doctrine) ≈ 3 (craft-rule) > 1 (already grounded) > 4 (UI) > bonus (persona-sim).

## Earning sequence to characterized-tier (illustrative, NOT prescriptive)

If three independent arcs ground the operator across three distinct work-shapes with characterized-tier evidence, the candidacy reaches characterized-tier and is eligible for Sapphire firing AT THAT POINT (not before). Illustrative sequence:

1. **Today (2026-05-07):** Work-shape 1 grounded at sketch-tier. Operator articulation locked. (Earned.)
2. **Future arc N+1:** A doctrine-strengthening arc applies the operator and earns Work-shape 2 grounding at sketch-tier or above.
3. **Future arc N+2:** A commit-derivation that uses the operator AS load-bearing reasoning earns Work-shape 5 grounding.
4. **Future arc N+M:** A craft-rule registry decision (most reachable from existing infrastructure) earns Work-shape 3 grounding.

At that point the candidacy has 3+ distinct work-shapes grounded; characterized-tier may be reachable; Sapphire firing depends on additional criteria (substantive distinctness sustaining; lived-behavior verification across surfaces; canonical synthesis artifact).

## What this map does NOT do

- This is NOT a runlist. Each future arc earns its grounding through honest work in its own domain; the map points at where grounding could come from.
- This map does NOT preempt-claim that any specific future arc will produce grounding evidence. The conditional form is *falsifiable*; future arcs might supply F-merge or F-collapse evidence and refute the candidacy.
- This map does NOT bind future sessions to pursue these work-shapes. The candidacy may go cold; future work may pursue different Sapphires entirely; both outcomes are honest.
- This map does NOT preempt apparatus-honest dry-well refusals. If a future arc tries to ground the operator and finds the parent operator's vocabulary sufficient by reformulation, that arc closes via dry-well exit on F-merge falsifier — not by silent refinement.

## Refusal carve-outs

- Do NOT cite this map as Sapphire-tier evidence. The map is methodology-prep; future arcs supply the evidence.
- Do NOT inflate "the map exists" into "the candidacy is well-positioned." The candidacy is sketch-tier; the map is doctrinal scaffolding.
- Do NOT use the map to justify modifying MISSION_FORMULA before earning. The formula's source-of-truth changes only when characterized-tier earning lands.
- Do NOT collapse "failure to ground in domain X" into "the operator is wrong." Domain-specific groundings can fail without refuting the operator universally; F-merge / F-unmeasurable / F-no-prediction must be observed across multiple domains for honest collapse.

## Composes with

- The operator formal definition (`2026-05-07-1200`) — names the operator this map roadmaps the grounding of.
- The Conditional Lens probe-specific findings (`2026-05-07-1140`) — Work-shape 1's sketch-tier grounding example.
- CLAUDE.md "Calibrated disciplines drift fast" — Work-shape 2 (doctrine) is the canonical surface where the operator could measure drift before it happens.
- CLAUDE.md "Craft-rules registry — substrate ⊥ apparatus as architecture" — Work-shape 3's existing infrastructure (the registry's tier vocabulary) is what the operator would refine.
- CLAUDE.md "UI boundary truth" — Work-shape 4's parent doctrine.
- CLAUDE.md "Commit messages include a Formula derivation" — Work-shape 5's surface (commit-derivations are a project-recognized reasoning-genre).

## Final read

`compensation_tax_w(t)` candidacy has a roadmap. Today's grounding (Work-shape 1, sketch-tier) is preserved. Future arcs in distinct domains can compound the candidacy across the named work-shapes. The map is reachability + threshold per domain, not a checklist.

The patience-shaped Sapphire stays patience-shaped. The substantive next-move belongs to future sessions.

Soli Deo gloria.
