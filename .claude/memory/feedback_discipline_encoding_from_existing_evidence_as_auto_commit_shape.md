---
name: Discipline-encoding-from-existing-evidence is a valid /auto-commit shape (zero-spend structural-enforcement-promotion arc)
description: Pattern discovered 2026-05-08 in previous /auto-commit 10 run — 10 moves, $0 API spend, pure structural-enforcement promotion of recently-earned discipline lessons across memory + skill-body + tooling tiers. Valid arc-shape when project has accumulated unfiled lessons faster than they were encoded.
type: feedback
---

When `/auto-commit N` is invoked after a high-density Sapphire-arc cluster (e.g., a day that fired 4 Sapphires + refused 4) and the new evidence + lessons haven't yet been encoded at memory/skill-body/tooling tiers per CLAUDE.md "Calibrated disciplines drift fast — promote to structural enforcement at earliest opportunity" doctrine, **discipline-encoding-from-existing-evidence is a valid arc shape** even though it produces zero API spend. The previous /auto-commit 10 run (2026-05-08 ~05:15-05:30, commits 538ce4e through 0480c7f) demonstrated this pattern.

**Why this matters as a NEW pattern (vs standard /auto-commit shape):**

The default /auto-commit-skill-body shape assumes substantive moves spend the fresh $5.00 budget on new evidence (worldcli ask / batch-hypotheses / live-corpus investigation / cross-character bite-test / etc.). The discipline-encoding shape is structurally different: zero new evidence; only PROMOTION of existing evidence across structural-enforcement tiers per the calibrated-disciplines-drift-fast doctrine.

The previous 10-move arc:
- 5 moves wrote feedback/project memories (memory tier)
- 1 move promoted patterns to /seek-sapphire-crown SKILL.md (skill-body tier)
- 1 move extracted reusable tooling module (tooling tier)
- 1 move retired a stale artifact per open-thread-hygiene doctrine
- 1 move named the substrate-already-produces lineage as a research program
- 1 move closing reflection

Total: 10 moves, $0 API spend, 9 commits + 1 closing-reflection-report-commit, all pushed to remote. The arc landed: tonight's discipline lessons are now durably encoded across three structural-enforcement tiers, the next-arc path is named and tooled, and the day's record stands.

**Why:** When the project's evidence-generation rate (Sapphire arcs producing N findings) outruns its encoding-rate (lessons getting written to memories / promoted to skill bodies / extracted to tooling), the gap creates **drift risk** — calibrated disciplines drift fast per CLAUDE.md doctrine, and unfiled lessons forget fast across sessions. Discipline-encoding /auto-commit runs close that gap deliberately. The auto-commit-skill body's "use the full instrument set" guidance includes encoding-tier promotion as much as evidence-generation.

**How to apply:** When invoking `/auto-commit N` after a high-density Sapphire-arc cluster, the arc shape can validly be discipline-encoding rather than evidence-generation if (a) substantial new lessons exist from the prior arcs, (b) those lessons haven't yet been encoded at memory + skill-body tiers, (c) tooling refinements would help the next-arc path. Acknowledge the shape choice in the run's opening sentence ("discipline-encoding-from-existing-evidence" or similar). Don't apologize for $0 spend — the spend was already paid in the prior arcs that produced the evidence; this arc is the structural-enforcement promotion that the calibrated-disciplines-drift-fast doctrine demands.

Composes with the /auto-commit skill body's "the arc landed somewhere" requirement — discipline-encoding arcs LAND on the structural-enforcement-promotion completion; the closing reflection names what tier-jumps happened. Composes with `feedback_codex_consult_discipline_maturation.md` (the pattern that the previous run encoded) as a worked example: codex-consult-discipline-maturation went from session-discovery (mid-arc Crown 13) → memory-entry (auto-commit Move 1) → skill-body discipline (auto-commit Move 6) in ~90 minutes via this shape. CLAUDE.md `Apparatus-honest correction loop` doctrine's Mode B (loop-close-preemptive) is a sibling pattern — Mode B audits for honest-debt at substantive-day loop-close; discipline-encoding /auto-commit promotes the audited-and-corrected lessons across structural-enforcement tiers.
