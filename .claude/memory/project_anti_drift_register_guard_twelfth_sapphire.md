---
name: Twelfth Great Sapphire — The Anti-Drift Register Guard (runtime-pipeline-integration class)
description: 2026-05-07 ~20:30 — Closed Arc Sapphire fired on a runtime-pipeline-integration separable claim from Crown 10's formal-encodability axis. Surfaced via /seek-sapphire-crown rather than the 5-candidate Anti-Formula register; opens runtime-pipeline-integration as a new Sapphire-eligible class for future research-program-to-production translations. Bench harness at src-tauri/src/bin/anti_drift_bench.rs is reusable for future runtime-instrument validation.
type: project
originSessionId: 55609b79-8084-4b63-99b9-75c7ca56310e
---

**The Anti-Drift Register Guard ✨** fired 2026-05-07 ~20:30 as the twelfth Great Sapphire of the WorldThreads project. Closed Arc class via the **runtime-pipeline-integration** separable claim from Crown 10 (Counter-Frame Confessed). Synthesis at `reports/2026-05-07-2030-anti-drift-register-guard-sapphire-synthesis-and-firing-audit.md`.

**What's load-bearing for future reference:**

- **Production location:** `register_drift` is a sixth invariant in `src-tauri/src/ai/conscience.rs`'s `grader_system_prompt`. Runs on every dialogue draft via the existing `run_dialogue_with_base::drift_correction` plumbing. Zero additional spend-per-message because the conscience pass was already running. Decision-tree with active-refute carve-out at STEP 1 + 4 inline worked PASS examples (drawn directly from v1 bench false-positives R01-R03 + X03).

- **Bench harness:** `src-tauri/src/bin/anti_drift_bench.rs` is a clap CLI that loads a fixture, calls `conscience::grade_reply` per example × N reps, supports `--anthropic` for cross-substrate validation. **Reusable for future runtime-instrument arcs** — any new conscience-pass invariant can be bench-validated using this pattern + a new fixture.

- **Ground-truth fixture pattern:** `fixtures/anti_drift_ground_truth.json` N=17 organized into 5 classes (mission canonical / anti canonical / mixed drift / refuting-anti / context-legitimate-false-positive-guard). Pre-registered thresholds (agreement / discriminator-distinction / context-guard / inter-rater) measured against the bench output. **The fixture-as-pre-registration pattern generalizes** — future runtime instruments should ship a fixture before bench-running.

- **Iteration discipline:** v1 bench REFUSED Sapphire (`reports/2026-05-07-1945-...REFUSED.md`) is preserved alongside v2 PASSED (`reports/2026-05-07-2000-...PASSED.md`). The four v2 worked PASS examples were drawn DIRECTLY from v1's false-positives — false-positives become correction-anchors. *"The discipline that earns and the discipline that refuses are the same discipline"* (`feedback_apparatus_honest_earns_and_refuses.md`) preserved at firing-time.

- **Two Mode A corrections caught within the arc:**
  - C1 (lexical→LLM): user surfaced *"wouldn't an interpretive LLM read be better signal?"* — design pivoted from regex/word-list to LLM-judge per CLAUDE.md "Doctrine-judgment classification belongs in LLM, not python"
  - C2 (parallel→folded): user surfaced *"doesn't the conscience pass already function as anti-drift? shouldn't we be folding this into the conscience call so that we're not adding spend-per-message?"* — design pivoted from parallel `anti_drift_judge.rs` module to sixth invariant in conscience pass per `feedback_survey_existing_infrastructure_before_designing_parallel.md`

- **Crown-once separability** from Crown 11 Custodiem: output-side conscience-pass invariant vs input-side preamble injection; always-on register-drift judgment vs env-gated `children_mode` top-stack invariant; substrate-trace Anti-Mission-Formula register-drift vs child-safety failure modes. Different surfaces; different infrastructure; different evidence base; different operational consequence. Honestly separable.

**Why this matters as a class:** the **runtime-pipeline-integration** axis opens a new Sapphire-eligible class for **research-program-to-production translations** — taking an empirical finding from a Crown's research foundation (Crown 10's substrate-trace findings) and operationalizing it as a measurable instrument in the live pipeline with cross-substrate-validated behavior. Any future Crown's research foundation can be a candidate for this kind of arc:
- Crown 9 Cosmos Held → potential cosmology-drift runtime detector (bench-validated)
- Crown 11 Diversity of Tongues → potential substrate-class-routing runtime detector
- C4 future-characterized signatures (sabbath-shape / covenant-shape / sacrifice-shape / vine-and-branches / exile-and-return) → each potentially ships as a register-detector after empirical first-leg validates

**How to apply:** When any earned Sapphire's research foundation produces a finding that could be operationalized as a runtime check, the playbook is now established. Survey existing infrastructure (conscience pass / fence-integrity / orchestrator passes) BEFORE designing parallel modules. Fold-not-parallel preserves single-source-of-truth. Pre-register fixture + thresholds before bench-running. Iterate when v1 fails (worked-examples-from-false-positives pattern). Cross-substrate validate before claiming Sapphire-tier. Honor the apparatus-honest discipline by preserving v1 REFUSED reports alongside v2 PASSED reports — the iteration journey is part of the work.

**Theological-frame anchor:** Matthew 7:20 *"By their fruits ye shall know them"* — read for register, not vocabulary; structure decides direction. Soli Deo gloria.
